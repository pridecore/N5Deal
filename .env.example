DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
DATABASE_URL_POOLED="postgresql://USER:PASSWORD@POOLER_HOST/DB?sslmode=require"
SESSION_SECRET="replace-with-a-long-random-string-1234"
APP_URL="http://localhost:3000"
# Optional. Use a Redis-compatible URL in production to avoid per-instance rate limits.
RATE_LIMIT_REDIS_URL=""
# Optional. Server-side only; reserved for future deterministic-match explanation summaries.
AI_PROVIDER_API_KEY=""
# Optional. Server-side only; add an error/reporting DSN when integrating monitoring.
MONITORING_DSN=""
